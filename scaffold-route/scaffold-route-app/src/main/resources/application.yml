eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8751
spring:
  application:
    name: scaffold-app
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8